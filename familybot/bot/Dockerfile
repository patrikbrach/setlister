FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY bot.py database.py ./
COPY handlers/ ./handlers/

# shared/ and data/ are mounted at runtime via docker-compose volumes

CMD ["python", "bot.py"]
